# ---------------------------------------------------------------
# Frontend environment configuration
# ---------------------------------------------------------------

# Mock mode (true) runs the app on built-in demo data with zero backend.
# Set to false to call the real FastAPI backend.
VITE_USE_MOCK=true

# Base URL of the FastAPI backend. The backend prefixes every route with /api.
VITE_API_BASE_URL=http://localhost:8000/api
